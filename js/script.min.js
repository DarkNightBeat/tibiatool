var tot, randomBg = Math.floor(18 * Math.random() + 1),
    src = "img/bg/" + randomBg + ".jpg",
    bool = !0,
    x = null,
    training = null,
    aviso = null;
setTimeout(function() {
	if($(window).width()>=767){
		    $("body").css("background", "url(" + src + ") center top");
		    $("body").css("background-size", "auto");
		    $("body").css("background-repeat", "no-repeat");
	}
	else{
		/*$("body").css("background", "#051021");*/
        $("body").css("background", "url(" + src + ") center top");
        $("body").css("background-size", "auto");
        $("body").css("background-repeat", "no-repeat");
	}
	 $("#num").html(parseInt($("#tr").val())), x = document.getElementById("audioalert"), training = document.getElementById("tr").value, aviso = document.getElementById("falt").value
}, 500), $("#tr").change(function() {
    $("#num").html(parseInt($("#tr").val())), training = document.getElementById("tr").value, aviso = document.getElementById("falt").value, check()
}), $("#falt").change(function() {
    $("#num").html(parseInt($("#tr").val())), training = document.getElementById("tr").value, aviso = document.getElementById("falt").value, check()
});
var p = null;

function check() {
    (training > 500 || training < 1) && ($("#tr").val(500), training = $("#tr").val()), (aviso > 499 || aviso < 1) && ($("#falt").val(499), aviso = $("#falt").val()), aviso >= training && ($("#falt").val(training - 10), aviso = $("#falt").val())
}

function start() {
    null == p ? (p = window.setInterval(go, 2e3), $(".start").html("Stop"), $("#tr").prop("disabled", !0), $("#falt").prop("disabled", !0)) : (window.clearInterval(p), p = null, $(".start").html("Start"), training = document.getElementById("tr").value, $("#num").html(parseInt($("#tr").val())), aviso = document.getElementById("falt").value, x.pause(), $("#tr").prop("disabled", !1), $("#falt").prop("disabled", !1))
}

function go() {
    tot = --training;
    if( tot <= aviso){
        x.play();
        window.blur();
        window.focus();
    }
    document.getElementById("num").innerHTML = tot;
    document.title = "TWC = " + tot;
}